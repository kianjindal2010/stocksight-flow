# Set these values in Vercel Project Settings → Environment Variables before deployment.
# Use a public https Gofile download or folder URL. The page will hide download buttons if this is missing or invalid.
VITE_DOWNLOAD_URL=
VITE_RELEASE_VERSION=Latest release
VITE_FILE_SIZE=108 MB
VITE_SHA256=
